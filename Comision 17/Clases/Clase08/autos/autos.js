let autos = require('./data/autos.json')

module.exports = {
    listar : autos,
    listarFor : (array) => {
        for (let i = 0; i < array.length; i++) {
        const element = array[i];
        }
    }
}

